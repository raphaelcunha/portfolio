---
interface Props {
  classProp?: string;
}

const { classProp } = Astro.props;
---

<div
  class:list={[
    "bg-card border- box-border flex rounded-4xl mix-blend-normal backdrop-blur-xl  transition-opacity duration-300 ease-in-out ",
    classProp,
  ]}
>
  <slot />
</div>
